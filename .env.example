# Copy this file to .env and fill in the values.
# .env is gitignored — never commit it.
#
# These credentials are used by nuget.config to authenticate against GitHub Packages.
# Use the dedicated machine account (see README → Installation → CI/CD).

NUGET_USERNAME=processgroup-bot
NUGET_READ_PAT=ghp_your_token_here
