# git-run
Small and simpe git .NET API

## Build requirements

`git-run` depends on the `ProcessGroup` NuGet package from GitHub Packages. The repository `nuget.config` contains the package source, but local builds and CI must provide GitHub Packages credentials with the `read:packages` scope.
